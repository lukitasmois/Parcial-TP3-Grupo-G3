package com.example.parcial_tp3_grupo_g3.ui.view.activities

import android.os.Bundle
import androidx.activity.enableEdgeToEdge
import androidx.appcompat.app.AppCompatActivity
import androidx.core.view.ViewCompat
import androidx.core.view.WindowInsetsCompat
import com.example.parcial_tp3_grupo_g3.R


class ActivityProfile : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.lay_frag_profile)
    }


    //val buttonPaymentDetails: Button = findViewById(R.id.buttonPaymentDetails)
    //val buttonReferralCode: Button = findViewById(R.id.buttonReferralCode)
    //val buttonSettings: Button = findViewById(R.id.buttonSettings)
    //val buttonLogout: Button = findViewById(R.id.buttonLogout)

}